import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.fluffybadgernudge',
  appName: 'fluffy-badger-nudge',
  webDir: 'dist'
};

export default config;
